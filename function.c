#include<stdio.h>
#include<math.h>


     float root()
     main()
     {
         float a,b,c,D,x,y,z=0;
         printf("enter the number");
         scanf("%d%d%d",a,b,c);
         z=a*x*x+b*x+c;
         D=b*b-4*a*c;
        x= (-b+sqrt(D))/2*a;
         y=(-b-sqrt(D))/2*a;
         return 0;

     }
     float root()
     {
         x= (-b+sqrt(D))/2a;
         y=(-b-sqrt(D))/2a;
         root(x,y);
         return 0;
     }

